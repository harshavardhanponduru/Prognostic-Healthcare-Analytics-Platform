// /api/templates/static/js/charts.js// Fetch historical vitals data and render chartasync function fetchHistoricalVitals() {    try {        const response = await fetch('/api/vitals/history');        const data = await response.json();        if (data.length === 0) {            console.log("❗ No historical data found.");            return;        }        // Extract data for chart        const timestamps = data.map(entry => entry.timestamp);        const heartRates = data.map(entry => entry.heart_rate);        const bloodPressures = data.map(entry => entry.blood_pressure);        // Create chart        const ctx = document.getElementById('vitalsChart').getContext('2d');        new Chart(ctx, {            type: 'line',            data: {                labels: timestamps,                datasets: [                    {                        label: 'Heart Rate (bpm)',                        data: heartRates,                        borderColor: '#e74c3c',                        backgroundColor: 'rgba(231, 76, 60, 0.1)',                        fill: true,                        tension: 0.1                    },                    {                        label: 'Blood Pressure (mmHg)',                        data: bloodPressures,                        borderColor: '#3498db',                        backgroundColor: 'rgba(52, 152, 219, 0.1)',                        fill: true,                        tension: 0.1                    }                ]            },            options: {                responsive: true,                plugins: {                    legend: {                        display: true,                        position: 'top'                    }                },                scales: {                    x: {                        title: {                            display: true,                            text: 'Time'                        }                    },                    y: {                        title: {                            display: true,                            text: 'Values'                        }                    }                }            }        });    } catch (error) {        console.error('❌ Error fetching historical data:', error);    }}// Fetch historical vitals on page loadwindow.onload = fetchHistoricalVitals;